# A régénérer avant le déploiement, ne pas commit la vraie valeur :
# kubectl create secret generic backend-secret --from-literal=JWT_SECRET=$(openssl rand -hex 32) --dry-run=client -o yaml
apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
type: Opaque
stringData:
  JWT_SECRET: change_this_secret_in_production
